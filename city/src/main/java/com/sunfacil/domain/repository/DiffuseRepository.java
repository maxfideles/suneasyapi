package com.sunfacil.domain.repository;

import com.sunfacil.domain.model.Diffuse;
import org.springframework.data.jpa.repository.JpaRepository;

public interface DiffuseRepository extends JpaRepository<Diffuse,Long> {

}
