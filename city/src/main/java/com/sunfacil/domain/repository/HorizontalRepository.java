package com.sunfacil.domain.repository;

import com.sunfacil.domain.model.Horizontal;
import org.springframework.data.jpa.repository.JpaRepository;

public interface HorizontalRepository extends JpaRepository<Horizontal,Long> {
}
