package com.sunfacil.domain.repository;

import com.sunfacil.domain.model.TiltedLat;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TiltedLatRepository extends JpaRepository<TiltedLat,Long> {
}
